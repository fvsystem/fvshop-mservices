apiVersion: v1
kind: ConfigMap
metadata:
  name: deployk8s-env-db-credential
  namespace: deployk8s
data:
    POSTGRES_PASSWORD: "${PASS_DB_CREDENTIAL}"
    POSTGRES_DB: "fvshop"
    POSTGRES_USER: "${USER_DB_CREDENTIAL}"